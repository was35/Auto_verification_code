# Auto Verification Code

自动接收手机验证码并填写到浏览器插件。

## 部署
1. 进入 `server` 目录。
2. 配置 `.env`。
3. 执行 `docker-compose up -d`。

## 插件安装
1. Chrome 浏览器打开 `chrome://extensions/`。
2. 开启“开发者模式”。
3. 点击“加载已解压的扩展程序”，选择 `extension` 目录。

## 手机设置
参考 `instructions` 目录下的文档。
