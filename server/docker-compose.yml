services:
  server:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-a_very_secret_key_12345}
      - AUTH_KEY=${AUTH_KEY:-admin123}
      - PYTHONUNBUFFERED=1
    restart: always
